# Mend Baseline Test

Temporary file to trigger a Mend SCA scan (file add satisfies Mend's scan-trigger criteria) and verify `displayMode: "baseline"` is active now that it's merged to `main`.

Delete before merging or closing this test PR.

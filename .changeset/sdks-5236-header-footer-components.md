---
'@forgerock/login-framework-cli': minor
---

Add `generate header <Name>` and `generate footer <Name>` commands, the matching `ping-lf` MCP server tools, and the header/footer component templates they scaffold. Scaffolding any header/footer component now also triggers a registry rebuild, mirroring the existing stage/callback behavior.

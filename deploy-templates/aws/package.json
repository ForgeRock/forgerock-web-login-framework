{
  "name": "@pingidentity/login-app-deploy-aws",
  "version": "0.0.0-preview",
  "private": true,
  "type": "module",
  "description": "AWS Lambda deployment template for the Ping Identity Login App (forward-compatible scaffold pending Effect HTTP refactor).",
  "scripts": {
    "deploy": "alchemy deploy",
    "dev": "alchemy dev",
    "destroy": "alchemy destroy",
    "logs": "alchemy logs"
  },
  "dependencies": {
    "@effect/platform-node": "4.0.0-beta.58",
    "alchemy": "2.0.0-beta.23",
    "effect": "4.0.0-beta.58"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
